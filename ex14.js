module.exports = function pairs(obj)
{
    let arr = []
    let finish = []
    let newObject = {}
    for (const elt of Object.entries(obj))
    {
        finish.push(elt)
        
        
    }
    return finish

}